<%- include('../partials/head') %>

<body id="dmRoot" class="dmRoot flex-site no-scroll-transition" style="background: #0f0f0f; color: white; margin: 0; min-height: 100vh; display: flex; align-items: center; justify-content: center;">
    <%- include('../partials/preloader') %>

    <main class="error-page generic-error">
        <div class="luxury-container">
            <div class="line-decorator"></div>
            
            <div class="error-header">
                <span class="error-category">System Exception</span>
                <h1 class="error-code"><%= typeof statusCode !== 'undefined' ? statusCode : 'ERROR' %></h1>
            </div>

            <div class="error-body">
                <p class="error-msg"><%= typeof msg !== 'undefined' ? msg : 'An unexpected inconsistency has occurred within the sanctuary servers.' %></p>
                
                <% if (process.env.NODE_ENV === 'development' && typeof err !== 'undefined' && err.stack) { %>
                    <div class="debug-panel">
                        <pre><%= err.stack %></pre>
                    </div>
                <% } %>
            </div>

            <div class="error-footer">
                <a href="/" class="luxury-link">Return to Sanctuary</a>
                <div class="support-text">Ambassadors Assembly • Technical Services</div>
            </div>
        </div>
    </main>

    <style>
        .luxury-container {
            max-width: 500px;
            width: 100%;
            padding: 60px;
            background: #ffffff;
            border: 1px solid rgba(23, 106, 96, 0.2);
            box-shadow: 0 20px 40px rgba(23, 106, 96, 0.05);
            border-radius: 24px;
            position: relative;
            text-align: left;
            color: #1a1a1a;
        }

        .line-decorator {
            position: absolute;
            top: 0;
            left: 0;
            width: 4px;
            height: 100px;
            background: linear-gradient(to bottom, var(--color_2, #176a60), transparent);
            border-top-left-radius: 24px;
        }

        .error-category {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 0.3em;
            color: var(--color_2, #176a60);
            font-weight: 800;
            display: block;
            margin-bottom: 20px;
        }

        .error-code {
            font-size: 5rem;
            font-weight: 900;
            margin: 0;
            line-height: 1;
            letter-spacing: -0.05em;
            color: #1a1a1a;
        }

        .error-msg {
            font-size: 1.1rem;
            line-height: 1.8;
            color: rgba(26, 26, 26, 0.7);
            margin: 30px 0 60px;
            font-family: 'AppleGaramond-LightItalic', serif;
            font-style: italic;
        }

        .luxury-link {
            color: #1a1a1a;
            text-decoration: none;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            font-size: 0.85rem;
            padding-bottom: 5px;
            border-bottom: 2px solid var(--color_2, #176a60);
            transition: 0.3s;
        }

        .luxury-link:hover {
            color: var(--color_2, #176a60);
            padding-left: 10px;
        }

        .error-footer {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
        }

        .support-text {
            font-size: 0.65rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            color: rgba(26, 26, 26, 0.4);
        }

        .debug-panel {
            margin: 20px 0;
            padding: 15px;
            background: #f8f9fa;
            border: 1px solid rgba(26, 26, 26, 0.1);
            border-radius: 8px;
            font-size: 10px;
            overflow-x: auto;
            color: var(--color_2, #176a60);
        }

        @media (max-width: 600px) {
            .luxury-container {
                padding: 40px 25px;
            }
        }
    </style>
</body>
</html>
