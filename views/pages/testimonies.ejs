<%- include('../partials/head') %>
<body id="dmRoot" class="dmRoot fix-mobile-scrolling flex-site dmResellerSite no-scroll-transition" style="padding: 0px; margin: 0px; background: var(--color_1);" data-flex-site="">
  
  <%- include('../partials/preloader') %>
  
  <div id="dm" class="dmwr">
    <div class="dm_wrapper -var5 null ">
      <div dmwrapped="true" id="1901957768" class="dm-home-page" themewaschanged="true">
        <div dmtemplateid="FlexHeader" data-responsive-name="FlexLayout" class="runtime-module-container dm-bfs dm-layout-home hasAnimations hasStickyHeader inMiniHeaderMode rows-1200 hamburger-reverse dmPageBody d-page-1716942098 inputs-css-clean dmDHeader" id="dm-outer-wrapper" data-page-class="1716942098" data-soch="true" data-background-parallax-selector=".dmHomeSection1, .dmSectionParallex">
          <div id="dmStyle_outerContainer" class="dmOuter">
            <div id="dmStyle_innerContainer" class="dmInner">
              <div class="dmLayoutWrapper standard-var dmStandardDesktop">
                <div id="site_content">

        <%- include('../partials/navbar') %>
<%- include('../partials/toasts') %>

<main class="testimonies-main" data-lenis-prevent-touch>
    <!-- Hero Section -->
    <section class="testimonies-hero">
        <img src="https://res.cloudinary.com/dxwhpacz7/image/upload/f_auto,q_auto/v1777418384/emerald-hero-bg.jpg" alt="Hero Background" class="hero-img">
        <div class="hero-mask"></div>
        <div class="testimonies-hero__content">
            
            <h1 class="hero-title" data-gsap="fade-up">God's <span class="text-italic">Glory</span></h1>
            <p class="hero-sub" data-gsap="fade-up" data-gsap-delay="0.1">Real stories of faith, healing, and restoration from our community.</p>
        </div>
    </section>

    <!-- Masonry Grid Section -->
    <section class="testimonies-grid-container">
        <div class="masonry-grid" id="testimonyMasonry">
            <% if (testimonies && testimonies.length > 0) { %>
                <% testimonies.forEach((testimony, index) => { %>
                    <div class="testimony-card" data-category="<%= testimony.category %>" data-gsap="masonry-reveal" onclick="openTestimonyModal('<%= testimony.id %>')">
                        <div class="quote-icon">“</div>
                        <p class="testimony-card__excerpt"><%= testimony.short_quote || testimony.content.substring(0, 150) + '...' %></p>
                        <div class="witness-info">
                            <div class="author__img">
                                <img src="<%= testimony.author_photo_url || '/images/default-avatar.png' %>" alt="<%= testimony.author_name %>">
                            </div>
                            <div class="author__info">
                                <h4 class="name"><%= testimony.author_name %></h4>
                                <span class="date"><%= testimony.category %></span>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-testimonies">
                    <p>No stories shared yet. Be the first to testify!</p>
                </div>
            <% } %>
        </div>

        <!-- Submission Trigger -->
        <div class="testimony-submission-cta" data-gsap="fade-up">
            <h2 class="cta-title">Have a <span class="text-italic">Story</span> to tell?</h2>
            <p>Your journey could be someone else's breakthrough.</p>
            <button class="share-btn" onclick="toggleSubmissionForm()">Share My Testimony</button>
        </div>
    </section>

    <!-- Slide-over Request Form -->
    <div class="testimony-form-overlay" id="testimonyForm">
        <div class="testimony-form-panel">
            <button class="close-panel" onclick="toggleSubmissionForm()">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </button>
            <div class="panel-content">
                <h2>Share the <span class="text-italic">Glory</span></h2>
                <p>Tell us how God has been moving in your life. Your story will be reviewed and shared to inspire others.</p>
                
                <form action="/testimonies/submit" method="POST" class="actual-form">
                    <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
                    <div class="hp-field" style="display:none !important; visibility:hidden !important;"><input type="text" name="full_name_hp" tabindex="-1" autocomplete="off"></div>
                    <% if (!locals.user) { %>
                    <div class="form-group">
                        <label>Your Name</label>
                        <input type="text" name="author_name" placeholder="How should we call you?" required>
                    </div>
                    <% } %>
                    <div class="form-group">
                        <label>Title of Testimony</label>
                        <input type="text" name="title" placeholder="e.g. A New Beginning" required>
                    </div>
                    <div class="form-group">
                        <label>Your Story</label>
                        <textarea name="content" placeholder="Share your experience..." rows="8" required></textarea>
                    </div>
                    <div class="form-checkboxes">
                        <label class="checkbox-label">
                            <input type="checkbox" name="is_anonymous">
                            <span>Post Anonymously</span>
                        </label>
                    </div>
                    <button type="submit" class="submit-btn">Share Testimony</button>
                </form>
            </div>
        </div>
    </div>

    <!-- Modal for full testimony -->
    <div class="testimony-modal" id="testimonyModal">
        <div class="testimony-modal__overlay" onclick="closeTestimonyModal()"></div>
        <div class="testimony-modal__content">
            <button class="modal-close" onclick="closeTestimonyModal()">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </button>
            <div id="modalBody"></div>
        </div>
    </div>
</main>

<style>
    :root {
      --aa-green: rgba(113, 178, 128, 1);
    }
    
    .text-green { color: var(--aa-green); }
    .text-italic { font-style: italic; font-family: 'AppleGaramond-LightItalic', serif; }

    .testimonies-main { background: var(--color_1); min-height: 100vh; color: #1a1a1a; }

    .testimonies-hero {
        height: 60vh;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        text-align: center;
        overflow: hidden;
    }
    
    .hero-img { position: absolute; top:0; left:0; width:100%; height:100%; object-fit:cover; opacity: 0.6; z-index: 1; }

    .hero-mask { position: absolute; bottom: 0; left: 0; width: 100%; height: 60%; background: linear-gradient(to bottom, transparent, var(--color_1)); z-index: 2; }
    .testimonies-hero__content { position: relative; z-index: 3; }
    .hero-title { font-size: clamp(3.5rem, 10vw, 7rem); font-weight: 950; line-height: 0.85; letter-spacing: -0.05em; margin-bottom: 30px; color: #1a1a1a; }
    .hero-sub { font-size: 1.25rem; color: rgba(26,26,26,0.6); max-width: 600px; margin: 0 auto; line-height: 1.6; }

    .testimonies-grid-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 60px 6% 150px;
    }

    .masonry-grid {
        columns: 3 350px;
        column-gap: 30px;
    }

    .testimony-card { 
      background: #ffffff; border: 1.5px solid rgba(26, 26, 26, 0.1); border-radius: 28px; padding: 40px; 
      margin-bottom: 25px; transition: 0.6s cubic-bezier(0.165, 0.84, 0.44, 1); cursor: pointer;
    }
    .testimony-card:hover { transform: translateY(-10px); border-color: var(--aa-green); box-shadow: 0 30px 60px rgba(0,0,0,0.05); }
    
    .quote-icon { font-size: 3rem; color: var(--aa-green); opacity: 0.2; margin-bottom: 20px; font-family: serif; }
    .testimony-card p { font-size: 1.1rem; line-height: 1.7; color: #1a1a1a; margin-bottom: 30px; font-style: italic; }
    .witness-info { display: flex; align-items: center; gap: 15px; }
    .author__img { width: 45px; height: 45px; border-radius: 50%; overflow: hidden; }
    .author__img img { width: 100%; height: 100%; object-fit: cover; }
    
    .read-more-btn {
        position: absolute; bottom: 30px; right: 30px;
        width: 40px; height: 40px; border-radius: 50%;
        background: transparent; border: 1.5px solid rgba(26,26,26,0.1);
        display: flex; align-items: center; justify-content: center;
        color: #1a1a1a; transition: 0.3s;
    }

    .testimony-card:hover .read-more-btn {
        background: var(--aa-green);
        border-color: var(--aa-green);
        color: white;
        transform: rotate(45deg);
    }

    /* CTA Section: Professional Depth */
    .testimony-submission-cta {
        margin-top: 100px;
        padding: 100px 6%;
        background: linear-gradient(135deg, rgba(23, 106, 96, 0.1) 0%, rgba(255, 255, 255, 0.9) 100%);
        border: 1px solid rgba(23, 106, 96, 0.2);
        border-radius: 50px;
        text-align: center;
        box-shadow: 0 20px 40px rgba(23, 106, 96, 0.05);
    }

    .share-btn {
        margin-top: 40px;
        background: linear-gradient(135deg, var(--aa-green) 0%, #115048 100%);
        color: white;
        padding: 20px 50px;
        border: none;
        border-radius: 100px;
        font-weight: 900;
        text-transform: uppercase;
        letter-spacing: 2px;
        cursor: pointer;
        transition: 0.4s;
        box-shadow: 0 10px 25px rgba(23, 106, 96, 0.3);
    }

    .share-btn:hover {
        transform: scale(1.05) translateY(-5px);
        box-shadow: 0 15px 35px rgba(23, 106, 96, 0.4);
    }

    /* Modal Styling */
    .testimony-modal__content {
        background: #ffffff;
        color: #1a1a1a;
        border: 1px solid rgba(23, 106, 96, 0.2);
        box-shadow: 0 25px 50px rgba(23, 106, 96, 0.1);
        padding: 40px;
        border-radius: 24px;
        max-width: 800px;
        width: 90%;
        position: relative;
    }
    .modal-close { color: #1a1a1a; opacity: 0.5; position: absolute; top: 20px; right: 20px; cursor: pointer; background: transparent; border: none; }
    .modal-close:hover { opacity: 1; color: var(--aa-green); }

    .testimony-form-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.6);
        z-index: 9999;
        display: flex;
        justify-content: flex-end;
        opacity: 0;
        visibility: hidden;
        transition: all 0.4s ease;
    }

    .testimony-form-overlay.is-active {
        opacity: 1;
        visibility: visible;
    }

    /* Floating Panel */
    .testimony-form-panel {
        background: #ffffff;
        color: #1a1a1a;
        width: 100%;
        max-width: 500px;
        height: 100%;
        border-left: 1px solid rgba(23, 106, 96, 0.2);
        box-shadow: -10px 0 30px rgba(23, 106, 96, 0.05);
        padding: 50px 40px;
        position: relative;
        transform: translateX(100%);
        transition: transform 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
        overflow-y: auto;
    }

    .testimony-form-overlay.is-active .testimony-form-panel {
        transform: translateX(0);
    }

    .actual-form input, .actual-form textarea {
        background: rgba(26,26,26,0.03);
        border: 1px solid rgba(26,26,26,0.1);
        color: #1a1a1a;
        border-radius: 12px;
        padding: 15px;
        width: 100%;
        margin-top: 5px;
        font-family: inherit;
    }
    .actual-form input:focus, .actual-form textarea:focus {
        border-color: var(--aa-green);
        outline: none;
        box-shadow: 0 0 0 3px rgba(23, 106, 96, 0.1);
    }
    .form-group { margin-bottom: 25px; text-align: left; }
    .form-group label { font-weight: 700; font-size: 0.95rem; color: #1a1a1a; display: block; margin-bottom: 8px; }
    .checkbox-label { display: flex; align-items: center; gap: 10px; font-weight: 600; cursor: pointer; color: #1a1a1a; }
    .submit-btn {
        width: 100%;
        background: linear-gradient(135deg, var(--color_2) 0%, #115048 100%);
        color: white;
        padding: 18px;
        border: none;
        border-radius: 12px;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 1px;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 10px 20px rgba(23, 106, 96, 0.2);
        margin-top: 30px;
    }
    .submit-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 15px 25px rgba(23, 106, 96, 0.3);
    }
    .close-panel { position: absolute; top: 25px; right: 25px; background: transparent; border: none; cursor: pointer; color: #1a1a1a; font-size: 2rem; display: flex; align-items: center; justify-content: center; width: 40px; height: 40px; border-radius: 50%; transition: background 0.3s; }
    .close-panel:hover { background: rgba(23, 106, 96, 0.1); color: var(--color_2); }

    /* Modal Overlay */
    .testimony-modal {
        position: fixed; top: 0; left: 0; width: 100%; height: 100%;
        display: flex; align-items: center; justify-content: center;
        opacity: 0; visibility: hidden; z-index: 10000;
        transition: 0.3s;
    }
    .testimony-modal.is-open { opacity: 1; visibility: visible; }
    .testimony-modal__overlay { position: absolute; width: 100%; height: 100%; background: rgba(0,0,0,0.8); cursor: pointer; }

    @media (max-width: 900px) {
        .masonry-grid { columns: 2; }
    }
    @media (max-width: 600px) {
        .masonry-grid { columns: 1; }
        .hero-title { font-size: 3rem; }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        // GSAP Staggered Reveal
        gsap.from('[data-gsap="masonry-reveal"]', {
            y: 50,
            opacity: 0,
            duration: 1,
            stagger: 0.15,
            ease: 'expo.out',
            scrollTrigger: {
                trigger: '.masonry-grid',
                start: 'top 80%',
            }
        });
    });

    function toggleSubmissionForm() {
        document.getElementById('testimonyForm').classList.toggle('is-active');
    }

    // High IQ: Handle Form Submission with reCAPTCHA
    document.querySelector('.actual-form').addEventListener('submit', async function(e) {
        e.preventDefault();
        const form = this;
        const btn = form.querySelector('.submit-btn');
        
        btn.disabled = true;
        btn.textContent = 'Verifying...';

        try {
            // Get reCAPTCHA token
            if (typeof grecaptcha !== 'undefined') {
                const token = await grecaptcha.execute('<%= locals.recaptchaSiteKey %>', { action: 'testimony_submit' });
                
                // Inject token into form
                let input = form.querySelector('input[name="g-recaptcha-response"]');
                if (!input) {
                    input = document.createElement('input');
                    input.type = 'hidden';
                    input.name = 'g-recaptcha-response';
                    form.appendChild(input);
                }
                input.value = token;
            }
            
            form.submit();
        } catch (error) {
            console.error('reCAPTCHA Error:', error);
            form.submit(); // Fallback to submit anyway, server will handle missing token if configured
        }
    });


    function openTestimonyModal(id) {
        // Find the card content
        const card = document.querySelector(`.testimony-card[onclick*="${id}"]`) || document.querySelector(`[onclick*="${id}"]`).closest('.testimony-card');
        const title = card.querySelector('.testimony-card__title').innerText;
        const excerpt = card.querySelector('.testimony-card__excerpt').innerText;
        // Since we don't have full content in the card usually, one would normally fetch it
        // But for this cinematic UI, we'll assume the excerpt was just a teaser
        
        document.getElementById('modalBody').innerHTML = `
            <h2 style="font-family: 'Manrope'; font-weight: 900; font-size: 2.5rem; margin-bottom: 30px;">${title}</h2>
            <div style="line-height: 1.8; font-size: 1.1rem; color: #444;">${excerpt}</div>
        `;
        
        document.getElementById('testimonyModal').classList.add('is-open');
        document.body.style.overflow = 'hidden';
    }

    function closeTestimonyModal() {
        document.getElementById('testimonyModal').classList.remove('is-open');
        document.body.style.overflow = '';
    }
</script>

<%- include('../partials/footer') %>
